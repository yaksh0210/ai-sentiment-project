name: Deploy Backend to Render

on:
  push:
    branches: [main]
    paths:
      - 'backend/**'

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Call Render Deploy Hook
        run: |
          curl -X POST https://api.render.com/deploy/srv-d1lr7ridbo4c73a8sme0?key=x0kdffDcqyU